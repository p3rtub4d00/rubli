services:
  - type: web
    name: rubli-api
    runtime: node
    rootDir: apps/api
    buildCommand: corepack enable && cd ../.. && pnpm install --frozen-lockfile && pnpm --filter @rubli/api build
    startCommand: node apps/api/dist/server.js
    healthCheckPath: /health
    autoDeploy: true
