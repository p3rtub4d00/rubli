services:
  - type: web
    name: rubli-api
    runtime: node
    buildCommand: corepack enable && pnpm install && pnpm --filter @rubli/shared build && pnpm --filter @rubli/api build
    startCommand: node apps/api/dist/server.js
    healthCheckPath: /health
    autoDeploy: true
