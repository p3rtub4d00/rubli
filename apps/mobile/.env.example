# Defina uma única URL para HTTP e WebSocket.
# Rede local: use o IP atual do computador (muda quando você troca de rede).
# Produção: use a URL HTTPS pública do serviço no Render.
EXPO_PUBLIC_RUBLI_API_URL=https://rubli-api.onrender.com
